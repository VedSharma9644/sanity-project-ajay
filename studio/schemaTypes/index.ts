import page from '../schemas/page'
import blockContent from '../schemas/blockContent'
import heroSection from '../schemas/heroSection'
import teamSection from '../schemas/teamSection'
import siteHeader from '../schemas/siteHeader'
import siteFooter from '../schemas/siteFooter'
import siteBlog from '../schemas/siteBlog'
import post from '../schemas/post'
import seoSection from '../schemas/seoSection'
import brands from '../schemas/brands'
import easyAccess from '../schemas/easyAccess'
import insightSection from '../schemas/insightSection'
import workspaceSection from '../schemas/workspaceSection'
import headingDescription from '../schemas/headingDescription'
import newsletterSection from '../schemas/newsletterSection'
import siteSettings from '../schemas/siteSettings'

export const schemaTypes = [
  page,
  blockContent,
  heroSection,
  teamSection,
  siteHeader,
  siteFooter,
  siteBlog,
  post,
  seoSection,
  brands,
  easyAccess,
  insightSection,
  workspaceSection,
  headingDescription,
  newsletterSection,
  siteSettings
]
